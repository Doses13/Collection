<Page
    x:Class="collectionTest1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:collectionTest1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" Background="#202020">
    <StackPanel x:Name="mainPage" Orientation="Vertical">
        <MenuBar>
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="Export"/>
                <MenuFlyoutItem Text="Exit"/>
            </MenuBarItem>
            <MenuBarItem Title="Edit">
                <MenuFlyoutItem Text="Add Item"/>
                <MenuFlyoutItem Text="Clear Collection"/>
            </MenuBarItem>
            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="About"/>
            </MenuBarItem>
        </MenuBar>
        <StackPanel x:Name="Home" Orientation="Horizontal">
            <ScrollViewer>
                <StackPanel x:Name="colButs" Background="#303030" Orientation="Vertical" Height="10000">
                    <!--collection buttons-->
                    <Button x:Name="colButton" Margin="10" Width="200" Click="NewCollectionFunc">New Collection (+)</Button>
                    <!-- this stays here -->

                </StackPanel>
            </ScrollViewer>
            <Grid x:Name="ItemGrid" Width="500" >
                <!--item view-->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Rectangle Grid.Row="0" Grid.Column="0" Fill="Red" Height="50" Margin="4" />
                <Rectangle Grid.Row="0" Grid.Column="1" Fill="AliceBlue" Height="50" Margin="4"/>
                <Rectangle Grid.Row="1" Grid.Column="1" Fill="Aqua" Height="50" Margin="4"/>
                <Rectangle Grid.Row="2" Grid.Column="0" Fill="Aqua" Height="50" Margin="4"/>
                <Rectangle Grid.Row="3" Grid.Column="3" Fill="Aqua" Height="50" Margin="4"/>
            </Grid>
        </StackPanel>
    </StackPanel>
</Page>
