<Page
    x:Class="collectionTest1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:collectionTest1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical">
        <MenuBar>
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="Export"/>
                <MenuFlyoutItem Text="Exit"/>
            </MenuBarItem>
            <MenuBarItem Title="Edit">
                <MenuFlyoutItem Text="Add Item"/>
                <MenuFlyoutItem Text="Clear Collection"/>
            </MenuBarItem>
            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="About"/>
            </MenuBarItem>
        </MenuBar>
        <StackPanel Orientation="Horizontal">
            <ScrollViewer>
                <StackPanel x:Name="colButs" Orientation="Vertical">
                    <!--collection buttons-->
                    <Button Margin="10">New Collection</Button>
                    <Button Margin="10">collection 2</Button>
                    <Button Margin="10">collection 3</Button>
                    
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </StackPanel>
</Page>
