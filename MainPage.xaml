<Page
    x:Class="collectionTest1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:collectionTest1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" Background="#202020">
        <StackPanel x:Name="mainPage" Orientation="Vertical">
            <MenuBar>
                <MenuBarItem Title="File">
                    <MenuFlyoutItem Text="Export"/>
                    <MenuFlyoutItem Text="Exit"/>
                </MenuBarItem>
                <MenuBarItem Title="Edit">
                    <MenuFlyoutItem Text="Add Item"/>
                    <MenuFlyoutItem Text="Clear Collection"/>
                </MenuBarItem>
                <MenuBarItem Title="Help">
                    <MenuFlyoutItem Text="About"/>
                </MenuBarItem>
            </MenuBar>
            <StackPanel x:Name="Home" Orientation="Horizontal">
                <ScrollViewer>
                    <StackPanel x:Name="colButs" Background="#303030" Orientation="Vertical">
                        <!--collection buttons-->
                        <Button x:Name="colButton" Margin="10" Width="200" Click="NewCollectionFunc">New Collection (+)</Button>
                        <!-- this stays here -->

                    </StackPanel>
                </ScrollViewer>
                <Grid x:Name="ItemGrid" Width="500" ColumnSpacing="25" RowSpacing="25">
                    <!--item view--> 
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                    </Grid.RowDefinitions>
                    <Button x:Name="addButton" Click="newItemFunc" Height="100" Width="100" HorizontalAlignment="Center">
                        <TextBlock Text="Add Item (+)" />
                    </Button>
                   
                </Grid>
            </StackPanel>
        </StackPanel>
        <StackPanel x:Name="addItem" Orientation="Vertical" Visibility="Collapsed">
            <TextBox x:Name="itemName" Header="Enter item name" PlaceholderText="Name" Width="300" HorizontalAlignment="Left" Margin="25" Visibility="Visible"/>
            <TextBox x:Name="itemDescription" Header="Enter item Description" PlaceholderText="Description" Width="400" Height="200" Margin="20" Visibility="Visible" HorizontalAlignment="Left"/>
            <TextBox x:Name="itemPrice" Header="Enter Price" PlaceholderText="Price" Width="150" Height="75" Margin="20" Visibility="Visible" HorizontalAlignment="Left"/>
            <TextBox x:Name="itemCondition" Header="Enter Conditoion" PlaceholderText="Condition" Width="150" Height="75" Margin="20" Visibility="Visible" HorizontalAlignment="Left"/>
            <Button x:Name="addItemConfirm" Content="Add Item" Click="newItemConfirm" Width="75" Height="25"/>
            <Button x:Name="addItemImage" Content="Add Image" Height="25" Width="50" Click="addImageInAddItem"/>

        </StackPanel>
        <Grid x:Name="addCollection" Visibility="Collapsed">
            <TextBox x:Name="colName" Grid.Row="0" Grid.Column="0" Header="Enter Collection Name" PlaceholderText="Name" Width="300" HorizontalAlignment="Left" Margin="25"/>
            <TextBox x:Name="attText" Grid.Row="2" Grid.Column="0" Header="Enter an Attribute" PlaceholderText="Attribute" Width="200" HorizontalAlignment="Left" Margin="25"/>
            <Button x:Name="addAtt" Grid.Row="5" Grid.Column="5"/>
            <Button x:Name="subAtt" Grid.Row="5" Grid.Column="8"/>
        </Grid>
    </StackPanel>
</Page>
